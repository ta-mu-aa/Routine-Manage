parameters:
  - name: routine_id
    in: path
    required: true
    schema:
      type: string
    description: ルーティンID
requestBody:
  content:
    application/json:
      schema:
        type: object
        required:
          - name
          - daily_target
        properties:
          name:
            type: string
          daily_target:
            type: integer